import assert from 'node:assert/strict'
import test from 'node:test'
import {generatePriorArtStrategies,scorePriorArtCandidate} from '../src/lib/prior-art-service.js'

test('builds multiple complementary prior-art strategies',()=>{const strategies=generatePriorArtStrategies({summary:'Encrypted model inference at an edge device',concepts:['encrypted inference','edge model'],cpc:['G06N20/00'],applicant:'Example Corp'});assert.ok(strategies.some(item=>item.strategy_type==='keyword'));assert.ok(strategies.some(item=>item.strategy_type==='exact_phrase'));assert.ok(strategies.some(item=>item.strategy_type==='classification'));assert.ok(strategies.some(item=>item.strategy_type==='applicant'));assert.ok(strategies.some(item=>item.strategy_type==='semantic'))})
test('rewards pre-critical accessible references with stronger technical coverage',()=>{const strong=scorePriorArtCandidate({criticalDate:'2022-01-01',publicationDate:'2020-01-01',technicalSimilarity:90,claimCoverage:80,familyRelevance:50,citationRelevance:40,accessible:true}),late=scorePriorArtCandidate({criticalDate:'2022-01-01',publicationDate:'2023-01-01',technicalSimilarity:90,claimCoverage:80,familyRelevance:50,citationRelevance:40,accessible:true});assert.equal(strong.timing_status,'pre_critical');assert.equal(late.timing_status,'post_critical');assert.ok(strong.quality_score>late.quality_score)})
test('labels missing publication timing as unknown instead of assuming prior art',()=>{const result=scorePriorArtCandidate({criticalDate:'2022-01-01',technicalSimilarity:80});assert.equal(result.timing_status,'unknown');assert.ok(result.quality_score<80)})
test('accepts database Date objects for critical dates',()=>{const result=scorePriorArtCandidate({criticalDate:new Date('2022-01-01T00:00:00Z'),publicationDate:'2020-01-01'});assert.equal(result.timing_status,'pre_critical')})
