import {neon} from '@neondatabase/serverless'
import {getSessionUser} from '../src/lib/auth.js'
import {addClearanceCandidate,createClearanceProject,getClearanceProject,listClearanceProjects,reviewClearanceCandidate,screenMatterTrademarks} from '../src/lib/trademark-clearance-service.js'

export default async function handler(req,res){const sql=neon(process.env.DATABASE_URL);try{const user=await getSessionUser(sql,req.headers.cookie);if(!user)return res.status(401).json({error:{message:'Not authenticated'}});if(req.method==='GET')return res.status(200).json(req.query?.project_id?await getClearanceProject(sql,user.id,req.query.project_id):await listClearanceProjects(sql,user.id,req.query?.matter_id));if(req.method!=='POST')return res.status(405).json({error:{message:'Method not allowed'}});const body=req.body||{},actions={create:()=>createClearanceProject(sql,user.id,body),screen_matter:()=>screenMatterTrademarks(sql,user.id,body.project_id),add_candidate:()=>addClearanceCandidate(sql,user.id,body),review_candidate:()=>reviewClearanceCandidate(sql,user.id,body)};if(!actions[body.action||'create'])throw new Error('Unknown clearance action');return res.status(body.action==='create'?201:200).json(await actions[body.action||'create']())}catch(error){return res.status(400).json({error:{message:error.message||'Trademark clearance operation failed'}})}}
