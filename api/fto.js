import {neon} from '@neondatabase/serverless'
import {getSessionUser} from '../src/lib/auth.js'
import {addFtoDesignAround,addFtoPatentReview,createFtoProject,finalizeFtoProject,getFtoProject,listFtoInputs,reviewFtoFeature,reviewFtoMapping,reviewFtoPatent,updateFtoCoverage} from '../src/lib/fto-service.js'
export default async function handler(req,res){const sql=neon(process.env.DATABASE_URL);try{const user=await getSessionUser(sql,req.headers.cookie);if(!user)return res.status(401).json({error:{message:'Not authenticated'}});if(req.method==='GET')return res.status(200).json(req.query?.project_id?await getFtoProject(sql,user.id,req.query.project_id):await listFtoInputs(sql,user.id,req.query?.matter_id));if(req.method!=='POST')return res.status(405).json({error:{message:'Method not allowed'}});const body=req.body||{},actions={create:createFtoProject,add_patent:addFtoPatentReview,review_feature:reviewFtoFeature,update_coverage:updateFtoCoverage,review_mapping:reviewFtoMapping,review_patent:reviewFtoPatent,add_design_around:addFtoDesignAround,finalize:finalizeFtoProject},action=actions[body.action];if(!action)return res.status(400).json({error:{message:'Unknown FTO action'}});return res.status(201).json(await action(sql,user.id,body))}catch(error){return res.status(400).json({error:{message:error.message||'FTO operation failed'}})}}
